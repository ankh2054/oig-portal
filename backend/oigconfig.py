#!/usr/bin/env python
import preprocessing

postgres = {
    "postgres-db": "oig",
    "postgres-host": "localhost",
    "postgres-port": "5432",
    "postgres-user": "oig",
    "postgres-pass": "nightshade900"
}

